---
description: 论文解读博客的写作规范与数学公式渲染注意事项。适用于在 _posts/论文阅读/ 目录下撰写论文解读文章。
globs:
  - "_posts/论文阅读/**/*.md"
alwaysApply: false
---

# 论文解读博客规范

## 1. 内容要求

请详细阅读论文，对论文进行全方位、深层次的细节展开讨论：

### 1.1 基础信息梳理
明确论文核心研究方向、研究背景、作者团队及核心研究目标，无需冗余，精准提炼关键信息。

### 1.2 细节深度展开（核心要求）
逐模块拆解论文内容，重点深入讨论以下细节，不跳过任何关键技术点、实验设计、公式推导及结论细节，做到"有细节、有分析、有解读"，而非简单复述：

- **研究方法/技术路线**：详细拆解论文提出的核心方法、技术框架；若有公式，需解释公式中每个参数的含义、公式的作用；说明该方法与现有方法的区别、创新点，以及作者选择该方法的原因；若有实验设计，需明确实验数据集、实验参数、对比模型、评价指标，解读实验设计的合理性。
- **核心实验结果**：深入分析论文中的实验数据、图表，解读每个实验结果背后的逻辑；说明实验结果如何支撑论文的核心观点；对比不同实验组的差异，分析实验结果的局限性（若论文有提及，需重点展开；若未提及，需结合领域常识补充合理分析）。
- **关键结论与争议点**：提炼论文的核心结论，深入讨论结论的合理性、适用场景；结合领域内相关研究，分析该结论的理论价值和实际应用价值；若论文存在争议点、未解决的问题，或与同类研究存在分歧，需重点展开讨论，给出合理分析和观点。
- **细节延伸**：针对论文中的关键细节（如特定技术的实现逻辑、实验中的异常数据、公式推导的关键步骤），结合领域常识和相关研究，进行延伸解读，补充细节背后的原理，帮助更全面理解论文。

### 1.3 格式要求
最终文档放在 `_posts/论文阅读/` 目录下，完全对齐已有论文解读的格式。

### 1.4 讨论要求
语言严谨、专业，贴合论文所属领域的表述习惯，深入且不空洞，避免泛泛而谈。针对细节的讨论要具体，比如分析某一技术的优势时，需结合论文中具体设计的细节说明，而非单纯说"该技术效果好"。若有不明确的细节，可基于论文内容合理推测，并标注"推测基于论文XX部分"。

## 2. 数学公式渲染规范

以下规则用于避免 Jekyll（kramdown + MathJax/KaTeX）的渲染冲突，**必须严格遵守**：

### 2.1 行内公式中的下划线
行内公式 `$...$` 中的所有下划线一律转义为 `\_`。这样不用去数一段话里到底有几个下划线，也不影响 MathJax/KaTeX 的正常渲染。

示例：
- 正确：`$s\_i$`、`$\mathcal{L}\_{\text{total}}$`
- 错误：`$s_i$`、`$\mathcal{L}_{\text{total}}$`

### 2.2 行内公式中的星号
行内公式 `$...$` 中的星号 `*` 必须写成 `\ast`。原因：kramdown 会在 MathJax 之前解析 markdown，将成对的 `*` 配对为斜体标记并消费掉，导致公式中 `^{*}` 变成 `^{}`，渲染失败。

示例：
- 正确：`$x^{\ast}$`
- 错误：`$x^{*}$`

> 注意：`$$` 块公式不受此影响，仅行内公式需要处理。

### 2.3 行内公式中的花括号和竖线
行内公式 `$...$` 中：
- 花括号 `\{` `\}` 必须写成 `\lbrace` `\rbrace`
- 绝对值/集合大小的竖线 `|` 必须写成 `\lvert` `\rvert`

原因：kramdown 会先于 MathJax 解析，`\\{` 中的 `\\` 可能被识别为换行，`|` 可能被识别为表格列分隔符，尤其当花括号与下划线紧邻（如 `\\}_{`）时极易出错。

示例：
- 正确：`$\lbrace x\_1, x\_2 \rbrace$`、`$\lvert S \rvert$`
- 错误：`$\{x_1, x_2\}$`、`$|S|$`

> 注意：`$$` 块公式不受此影响，仅行内公式需要处理。

## 3. Front Matter 格式

```yaml
---
categories: [机器学习]
tags: [推荐系统, ...]  # 根据论文内容选择合适的 tags
math: true
title: "论文标题的中文翻译/解读标题"
---
```

## 4. 文章结构模板

```markdown
**论文**: 英文论文标题
**链接**: [https://arxiv.org/abs/xxxx.xxxxx](https://arxiv.org/abs/xxxx.xxxxx)
**机构**: 作者机构
**作者**: 作者列表
**发表**: 会议/期刊名 年份
**时间**: 年月

---

## 1. 基础信息与核心目标
...

## 2. 方法详解
### 2.1 ...
...

## 3. 实验分析
### 3.1 ...
...

## 4. 关键结论与讨论
...
```
